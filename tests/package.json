{
  "name": "cv-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for Adrian Wedd CV site",
  "scripts": {
    "test": "npm run test:json && npm run test:smoke",
    "test:json": "node validate-json.test.js",
    "test:smoke": "npx playwright test smoke.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  }
}
